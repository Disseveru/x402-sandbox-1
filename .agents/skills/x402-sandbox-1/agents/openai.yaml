interface:
  display_name: "X402 Sandbox 1"
  short_description: "Repo-specific patterns and workflows for x402-sandbox-1"
  default_prompt: "Use the x402-sandbox-1 repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true